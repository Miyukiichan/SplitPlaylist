# SplitPlaylist
## TODO

- Custom file name format
- Prepend index numbers with suitable amount of '0's
- Make use of the Artist field somehow
- Possibly add an Album field
- Checkbox for replace existing tracks generated by ffmpeg yes/no
- Option to use youtube-dl or ffmpeg installed on the system rather than bundled exes
  - Add controller function for launching programs such as youtube-dl or ffmpeg - makes this and other things easier
- Make for look bootiful
- Improve exception handling in the UI
- Introduce command line app version using controller library
- Tidy up controller a bit
- Have optional limit to amount of ffmpeg processes at a time for batch processing rather than all at once - opens up option for sequential processing
- Capture youtube-dl stdout and put it in the statusbar rather than launching a terminal
- Possibly put a sleep when checking all the processes so to not overload the CPU
- TEST WITH DIFFERENT DATA
